interface:
  display_name: "EnVault"
  short_description: "Use scoped credentials through a safe broker"
  default_prompt: "Use $envault to discover secret metadata or launch a child process with injected credentials without exposing plaintext credentials to yourself."
