interface:
  display_name: "EnVault"
  short_description: "Use scoped credentials through a safe broker"
  default_prompt: "Use $envault to perform one policy-bounded authenticated operation without exposing plaintext credentials."
